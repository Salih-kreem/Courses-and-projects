[#] String Method {1}:-

    let name = "Salih ";

    1- Access with Index.
       Notes: Index start from 0.
       console.log(name[2]); return l.

    2- Access with charAt();
       console.log(name.charAt(1)); return a.

    3- length // هو طول العنصر وتبدا برقم 1
       console.log(name.length) return 6.

    4- trim(); // تقص المسافات الموجوده في العنصر
       console.log(name.trim());

    5- toUpperCase();
       console.log(name.toUpperCase()); return SALIH.

    6- toLowerCase();
       console.log(name.toLowerCase()); return salih.

    7- Chain Method:-
       console.log(name.trim().charAt(2).toUpperCase()); return L.






[#] String Method {2}:-

    let name = "salih alnor gadkreem";

    1- indexOf(); // يبحث في العنصر من البداية

       indexOf(value[mand], start[opt]); if you don't write start will be 0.
       console.log(name.indexOf("a")) return 1.
       if value not found return -1

    2- lastIndexOf();

       lastIndexOf(value[mand], start[opt] length);
       console.log(name.lastIndexOf("a")); return 13.
       if the value not found return -1.

    3- slice(); // تقوم بقطع المكان المحدد

       slice(start[mand], end[opt]);
       If you dont defind end will be continue to the end element.
       notes: Not Including End.
       console.log(name.slice(5, 11)) return alnor.
       console.log(name.slice(-5)) return dkreem.
       console.log(name.slice(-7 , -3)); return gadk.

    4- split(); // يقص العنصر ويسترجع مصفوفه

       split(separetor[opt], limit[opt])
       console.log(name.split("")); split all the letters.
       console.log(name.split(" ")); split from the spaces.

    5- repeat(); // يكرر العنصر على حسب الرقم المكتوب
       console.log("@".repeat(5)); return @@@@@





[#] String Method{3}:-
    1- substring(); // تقطع مكان معين من النص
       substring(start[mand], end[opt]); Not Including End
       Notes:
          - If start > end will do swaping.
          - If srart < 0 start from 0.
          - Us length to get last Character.

   2- substr();
      substr(start[mand], character to Extract);
      Notes:
         - start >= length = " ".
         - Negative start from End.
         substr(5, 4);
         substr(-5, 4);

   3- includes(); {ES6} // هل يحتوي على القيمة
      includes(value[mand], start[opt]); Default 0.
      console.log(name.includes("salih"));
      Notes: If value find return {true} else {false}.

   4- startsWith(); {ES6} // هل يبدا بهذة القيمة
      startsWith(value[mand], start[opt]); Default 0.
      console.log(name.startsWith("s")); return true

   5- endsWith(); {ES6} // هل ينتهي بهذة القيمة
      endsWith(value[mand], length[opt]); Default full length.
      console.log(name.endsWith("s")); return false.
